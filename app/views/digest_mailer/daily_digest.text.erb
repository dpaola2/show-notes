Your library — <%= @date %>
<%= @total_count %> episode<%= 's' unless @total_count == 1 %> ready
========================================

<%= @featured_episode.podcast.title %>
<%= @featured_episode.title %>
========================================

<% @featured_episode.summary.sections.each do |section| %>
<%= section["title"] %>
<%= section["content"] %>

<% end %>
<% if @featured_episode.summary.quotes.any? %>
<% @featured_episode.summary.quotes.each do |quote| %>
> "<%= quote["text"] %>"
<% end %>

<% end %>
Read in app: <%= tracking_click_url(@click_events[@featured_episode.id][:summary].token) %>
<% if @recent_episodes.any? %>

----------------------------------------
LATEST EPISODES
----------------------------------------
<% @recent_episodes.each do |episode| %>

<%= episode.podcast.title %>
<%= episode.title %>
<% if episode.summary.present? %>
<%= truncate(episode.summary.sections.first["content"], length: 200) %>
<% end %>
Read full summary: <%= tracking_click_url(@click_events[episode.id][:summary].token) %>
Listen: <%= tracking_click_url(@click_events[episode.id][:listen].token) %>
<% end %>
<% end %>

----------------------------------------
That's all for now.

Open Show Notes: <%= root_url %>

You're receiving this because you have daily digest enabled.
Manage your settings to unsubscribe: <%= settings_url %>
